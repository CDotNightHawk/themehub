# Paw Mini-screen

`layout.json` is a generic widget layout consumable by most USB mini-screen
daemons (Turing Smart Screen, AIDA64 panels, or a DIY Python driver).
Dimensions are 480×320 — adjust `width`/`height` for other resolutions.

# Ventoy Paws

Drop the unzipped folder into `~/ventoy/themes/ventoy-paws` and reference it
from your Ventoy plugin config:

```json
{
  "theme": {
    "file": "/themes/ventoy-paws/theme.txt"
  }
}
```

Or use the CLI: `themehub install ventoy-paws`.

# Paw Waybar

Copy this folder into `~/.config/waybar/` or point Waybar at it:

```
waybar -c ~/.config/waybar/themes/paw-waybar/config.jsonc \
       -s ~/.config/waybar/themes/paw-waybar/style.css
```

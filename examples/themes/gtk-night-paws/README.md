# GTK Night Paws

Reference GTK 3/4 theme — a starting point for a violet-accent dark theme.
Real CSS lives in `gtk-3.0/gtk.css` and `gtk-4.0/gtk.css` once you flesh it
out; this seed includes only the manifest + index file so the upload is small.
